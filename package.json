{
  "name": "flappy-irys",
  "version": "1.0.0",
  "description": "Flappy Bird игра с общим лидербордом",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t flappy-irys .",
    "docker:run": "docker run -p 3000:3000 flappy-irys",
    "docker:compose": "docker-compose up -d",
    "docker:dev": "docker-compose --profile dev up -d"
  },
  "keywords": ["game", "flappy-bird", "leaderboard"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 